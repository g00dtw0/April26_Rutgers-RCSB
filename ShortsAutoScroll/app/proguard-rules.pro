# The accessibility service is instantiated by the system through the manifest,
# so keep it (and everything else in the app) addressable by name.
-keep class io.github.g00dtw0.shortsautoscroll.** { *; }
